import { BlockchainExplorer } from '../BlockchainExplorer';

export default function BlockchainExplorerExample() {
  return (
    <div className="min-h-screen bg-background p-8">
      <div className="max-w-4xl mx-auto">
        <BlockchainExplorer />
      </div>
    </div>
  );
}
